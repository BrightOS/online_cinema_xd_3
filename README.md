# 🎬 Online Cinema XD 3

Этот репозиторий содержит реализацию сервиса онлайн-кинотеатра, построенного на микросервисной архитектуре. Проект включает в себя полноценный стек для стриминга видео, поиска контента и глубокого мониторинга.

---

## 🚀 Ключевые фичи и ответственные

Проект разделен на функциональные блоки:

### 🔍 Поиск
- **Поисковый движок (ElasticSearch)**: Полнотекстовый поиск по фильмам и актерам.

### 💾 Данные
- **Хранение метаданных (PostgreSQL)**: Хранение информации о фильмах, актерах и жанрах.
- **ETL пайплайн**: Автоматическая синхронизация данных между PostgreSQL и ElasticSearch.

### 📊 Observability (Логи, метрики, трейсы)
- **Сбор метрик и логов (Prometheus)**: Мониторинг состояния системы.
- **Трейсинг (Jaeger)**: Распределенная трассировка запросов для отладки микросервисов.
- **Аналитика активности (Prometheus)**: Сбор данных о пользовательском поведении.
- **Дашборд аналитики (Grafana)**: Визуализация просмотров и активности пользователей.

### 📽 Стриминг и хранение
- **Потоковое вещание (HLS)**: Адаптивное стриминговое получение видеофайлов.
- **Хранение контента (S3/MinIO)**: Надежное хранение видеофайлов и статики.

---

## 🏗 Архитектура

Проект реализован на **микросервисах**, что обеспечивает масштабируемость и независимую разработку компонентов.

| Технология | Назначение |
|------------|------------|
| **FastAPI** | Основной фреймворк для микросервисов |
| **PostgreSQL** | Реляционная база для метаданных |
| **Elasticsearch** | Поисковый движок |
| **Redis** | Кэширование (опционально) |
| **MinIO** | S3-совместимое объектное хранилище |
| **Prometheus/Grafana** | Мониторинг и визуализация |
| **Jaeger** | Трейсинг |

---

## 🛠 Быстрый старт

Для управления проектом используется `Makefile`.

### Запуск всех сервисов
```bash
make up
```

### Остановка всех сервисов
```bash
make down
```

### Просмотр логов
```bash
make logs
```

---

## 📋 Доступные сервисы

| Сервис | URL / Порт | Описание |
|--------|------------|----------|
| **Grafana** | [http://localhost:3000](http://localhost:3000) | Дашборды и визуализация (`admin`/`admin`) |
| **Prometheus** | [http://localhost:9090](http://localhost:9090) | Сбор метрик |
| **Jaeger UI** | [http://localhost:16686](http://localhost:16686) | Трейсинг запросов |
| **MinIO Console** | [http://localhost:9001](http://localhost:9001) | Управление S3-хранилищем (`minioadmin`) |
| **Elasticsearch** | `9200` | Search API |
| **PostgreSQL** | `5432` | DB API |

---

## 📂 Структура проекта

```text
.
├── movies/             # Микросервис метаданных фильмов (PostgreSQL + Elastic)
├── videostream/        # Микросервис стриминга видео (HLS + S3)
├── deploy/             # Конфигурации инфраструктуры
│   ├── grafana/        # Настройки дашбордов и источников данных
│   └── prometheus/     # Правила сбора метрик
├── docker-compose.yml  # Оркестрация всех сервисов
├── Makefile            # Команды для быстрой работы
└── README.md           # Эта документация
```
